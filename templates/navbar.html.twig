<nav class="navbar navbar-expand-lg navbar-dark bg-primary text-white">
  <a class="navbar-brand" href="{{ path('compte') }}">My bank account</a>
    <div class="collapse navbar-collapse" id="navbarColor01">
      {% if app.user and ('ROLE_ADMIN' in app.user.roles ? app.user.roles|json_encode : '') %}
        <ul class="nav navbar-nav ml-auto">
          <li class="nav-item">
            <a class="nav-link" href="{{ path('user_index') }}">Tables des Utilisateurs</a>
          </li>
        </ul>
      {% endif %}
      <ul class="nav navbar-nav ml-auto">
        <li class="nav-item">
            {% if app.user %}
              <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
            {% else %}
              <meta http-equiv="refresh" content="1; URL={{ path('app_login') }}">
            {% endif %}
        </li>
      </ul>
    </div>
</nav>
