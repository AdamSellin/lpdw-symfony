{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}

{# inlude navbar #}
{% include 'navbar.html.twig' %}

<h1>Utilsateur</h1>

<form>
  <fieldset disabled>
    <div class="mb-3">
      <label class="form-label">Email</label>
      <input type="text" id="disabledTextInput" class="form-control" placeholder="{{ user.email }}">
    </div>
    <div class="mb-3">
      <label class="form-label">Password</label>
      <input type="text" id="disabledTextInput" class="form-control" placeholder="{{ user.password }}">
    </div>
  </fieldset>
   <div class="mt-3">
      <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-dark">Modifier</a>
      <a href="{{ path('user_index') }}" class="btn btn-dark">Listes Utilsateurs</a>
    </div>
</form>
{{ include('user/delete.html.twig') }}

{% endblock %}
